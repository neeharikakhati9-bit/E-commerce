import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "images.hindustantimes.com", "res.cloudinary.com"],
  },
};

export default nextConfig;
